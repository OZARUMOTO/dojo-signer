// Generated by `slint-keyos-platform` DO NOT EDIT!

import { HomePage } from "../pages/home/page.slint";
import { CoinjoinPage } from "../pages/coinjoin/page.slint";
import { PayNymPage } from "../pages/paynym/page.slint";
import { ReceivePage } from "../pages/receive/page.slint";
import { SendPage } from "../pages/send/page.slint";
import { SettingsPage } from "../pages/settings/page.slint";
import { UtxoPage } from "../pages/utxo/page.slint";
import { VerifyPage } from "../pages/verify/page.slint";
import { VaultPage } from "../pages/vault/page.slint";
import { RouteState, RouteOption } from "internal.slint";

// Mount this component in your app to enable routing
export component Router inherits Rectangle {
    if (RouteState.active == RouteOption.Home): HomePage {
    }
    if (RouteState.active == RouteOption.Coinjoin): CoinjoinPage {
    }
    if (RouteState.active == RouteOption.PayNym): PayNymPage {
    }
    if (RouteState.active == RouteOption.Receive): ReceivePage {
    }
    if (RouteState.active == RouteOption.Send): SendPage {
    }
    if (RouteState.active == RouteOption.Settings): SettingsPage {
    }
    if (RouteState.active == RouteOption.Utxo): UtxoPage {
    }
    if (RouteState.active == RouteOption.Verify): VerifyPage {
    }
    if (RouteState.active == RouteOption.Vault): VaultPage {
    }
}
