// Generated by `slint-keyos-platform` DO NOT EDIT!

export enum Animate {
    Forward,
    Backward,
    None,
}

struct NavigateOptions {
    // Replace the current route in the history stack
    replace: bool,
    
    // Animation to use when navigating
    animate: Animate,
}

export global Navigate {
    // Navigation state
    in property <bool> has-backward;
    in property <bool> has-forward;
    
    // Generic navigation callbacks
    callback backward();
    callback forward();
    
    callback backward-animate(Animate);
    callback forward-animate(Animate);
    
    callback return-home();
    callback return-home-animate(Animate);
    
    // Log the current navigation state
    callback debug();
    
    // Raw string navigation
    callback navigate(string, NavigateOptions);
    
    //
    // Type safe navigation callbacks
    //
    
    // 
    callback home(NavigateOptions);
    
    // /coinjoin
    callback coinjoin(NavigateOptions);
    
    // /paynym
    callback pay-nym(NavigateOptions);
    
    // /receive
    callback receive(NavigateOptions);
    
    // /send
    callback send(NavigateOptions);
    
    // /settings
    callback settings(NavigateOptions);
    
    // /utxo
    callback utxo(NavigateOptions);
    
    // /verify
    callback verify(NavigateOptions);
    
    // /vault
    callback vault(NavigateOptions);
}

export global RoutePath {
    out property <string> home : "";
    out property <string> coinjoin : "/coinjoin";
    out property <string> pay-nym : "/paynym";
    out property <string> receive : "/receive";
    out property <string> send : "/send";
    out property <string> settings : "/settings";
    out property <string> utxo : "/utxo";
    out property <string> verify : "/verify";
    out property <string> vault : "/vault";
}

